name: q2-b2-partial-relocation

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  b2:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install
        run: python -m pip install -e '.[test]'
      - name: Test
        run: pytest
      - name: Download frozen Q1 V10 anchor
        uses: actions/download-artifact@v4
        with:
          name: q1-exact-count-split-recombination-v10
          path: inputs/q1_v10_anchor
          github-token: ${{ github.token }}
          repository: HaochenLv/Math_exercise_1
          run-id: 34131373295
      - name: Run Q2 B2 partial relocation
        run: |
          python experiments/run_q2_b2_partial_relocation.py \
            --q1-routes inputs/q1_v10_anchor/q1-routes.csv \
            --q1-assignments inputs/q1_v10_anchor/q1-assignments.csv \
            --output-dir outputs/q2/b2_partial_relocation \
            --packing-time-limit 30 \
            --max-hosts-per-donor 32
      - name: Upload B2 outputs
        uses: actions/upload-artifact@v4
        with:
          name: q2-b2-partial-relocation
          path: outputs/q2/b2_partial_relocation
          if-no-files-found: error
